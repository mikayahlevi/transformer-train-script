{
    "sequence_length": 1024,
    "batch_size": 48,

    "total_steps": 50000,


    "update_every": 4,

    "schedule_every": 16,

    "log_loss_every": 32,

    "eval_every": 256,


    "print_progress_every": 40,

    
    "save_checkpoint_every": 1000
}